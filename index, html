<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>My Digital Portfolio</title>
  <style>
    :root {
      --bg: #fdfdfd;
      --text: #222;
      --primary: #6c63ff;
      --card: #fff;
    }
    body.dark { --bg:#1a1a2e; --text:#eee; --card:#252545; }
    body.blue { --primary:#007bff; }
    body.purple { --primary:#6c63ff; }
    body.gradient { --primary:linear-gradient(45deg,#ff6ec4,#7873f5); }

    * {margin:0;padding:0;box-sizing:border-box;font-family:"Poppins",sans-serif;}
    body {background:var(--bg);color:var(--text);transition:0.4s;overflow-x:hidden;}

    header {
      display:flex;justify-content:space-between;align-items:center;
      padding:1rem 2rem;position:sticky;top:0;background:var(--bg);z-index:1000;
    }
    header h1{color:var(--primary);}
    nav a{margin:0 1rem;text-decoration:none;color:var(--text);font-weight:500;}
